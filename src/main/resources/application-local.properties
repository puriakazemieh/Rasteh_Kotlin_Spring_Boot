# Local compatibility for a database created before Flyway. A fresh local database
# is created by migrations and then checked using `validate`, never `update`.
spring.jpa.hibernate.ddl-auto=validate
spring.flyway.baseline-on-migrate=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
