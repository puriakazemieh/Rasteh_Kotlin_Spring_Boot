# Local-only convenience profile. It is never selected by default or in release.
# Production keeps the base `validate` policy and uses reviewed migrations.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
